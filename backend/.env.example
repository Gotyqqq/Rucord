# Скопируйте в .env и заполните. Файл .env не должен попадать в git.

# Обязательно для production: длинная случайная строка (например: openssl rand -base64 32)
JWT_SECRET=your-long-secret-here

# ID пользователей с правами мастера (видят все серверы, полные права). Только для админов.
MASTER_USER_IDS=1

# Порт сервера (по умолчанию 3001)
# PORT=3001

# Ключ Giphy для поиска GIF (опционально)
# GIPHY_API_KEY=
