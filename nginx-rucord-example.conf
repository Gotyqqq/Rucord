# Пример конфига для rucrod.io
# Скопируй в /etc/nginx/sites-available/rucord и поправь пути к SSL-сертификатам

# Редирект HTTP → HTTPS
server {
    listen 80;
    server_name rucrod.io www.rucrod.io;
    return 301 https://$host$request_uri;
}

# HTTPS
server {
    listen 443 ssl;
    server_name rucrod.io www.rucrod.io;

    # Пути к сертификатам (как в панели; после выпуска серта для rucrod.io имена файлов могут быть rucrod.io_*.crt / .key)
    ssl_certificate /var/www/httpd-cert/www-root/rucrod.io_le2.crt;
    ssl_certificate_key /var/www/httpd-cert/www-root/rucrod.io_le2.key;

    root /var/www/www-root/data/root/Rucord/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /socket.io {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
